# Real demo recording

The bundled GIF is a clean animated walkthrough made from the current app UI. For a real click-by-click recording, use `⌘⇧5` on macOS, capture 12–18 seconds, then convert with ffmpeg.
