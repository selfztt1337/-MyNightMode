# GitHub showcase assets

- `hero-current.png` — hero based on the current V10 interface screenshot.
- `demo-current.gif` — short looping product demo built from the current interface.
- `app-current.jpg` — original current screenshot used in the README.

All paths are relative, so GitHub renders them directly from the repository.
